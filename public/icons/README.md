# PWA icons

Drop your PNG icons here:

- `icon-192.png` (192×192)
- `icon-512.png` (512×512)

An SVG source (`icon.svg`) is provided as a starting point. Convert it to the
required PNG sizes (e.g. via `sharp`, `pwa-asset-generator`, or any online
tool) before shipping.
